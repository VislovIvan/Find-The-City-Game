import SwiftUI

struct MultiplayerMenuView: View {
    var body: some View {
        VStack {
            Text("MP")
        }
        .navigationTitle("MultiplayerMenu")
    }
}

#Preview {
    MultiplayerMenuView()
}
